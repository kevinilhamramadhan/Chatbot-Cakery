# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Env / secrets
.env
*.env
!.env.example

# Local data
*.db
*.sqlite3
chroma_db/
chatbot-service/chroma_db/
chatbot-service/*.db

# OS / editor
.DS_Store
.idea/
.vscode/

# Reference backend is a separate repo, keep it out of this one's history
Backend-Cakery/

# Capstone reference docs (contain personal data: names/NIM) — keep out of repo
Dokumen capstone/

# Local Claude Code settings
.claude/

# WhatsApp gateway credentials — a logged-in session is a live account handle,
# and the pairing QR is just as sensitive. Never commit these.
whatsapp-gateway/sessions/
qr.png

# Fine-tune model binaries — live on HuggingFace (LasagnaS/toti-qwen-gguf), not git
*.gguf
*.gguf.*
finetune/gguf_dl/
# transient local backups
*.v1.json

# Modelfile hasil generate bootstrap (path GGUF absolut untuk container)
finetune/Modelfile.generated

# ── Hasil backup & arsip ─────────────────────────────────────────────────────
# DEPLOY_VPS.md §11 menyuruh menjalankan pg_dump / tar dari dalam folder repo,
# jadi berkas hasilnya gampang tidak sengaja ikut ter-commit.
*.dump
*.sql
*.sql.gz
*.tgz
*.tar
*.tar.gz
*.zip
*.bundle
sessions-*.tgz
static.tgz

# ── Berkas sementara & sisa merge/patch ──────────────────────────────────────
*.bak
*.orig
*.rej
*.tmp
*.part
nohup.out

# ── Cache alat Python (selain yang sudah di atas) ────────────────────────────
.mypy_cache/
.ruff_cache/
.tox/
.coverage
htmlcov/

# ── Node (wwebjs-api dikonfigurasi, bukan dibangun di sini — tapi jaga-jaga) ──
node_modules/
npm-debug.log*
yarn-error.log*

# ── Editor & OS ──────────────────────────────────────────────────────────────
*.swp
*.swo
*~
Thumbs.db
desktop.ini
.Trash-*

# Catatan: `*.log` sengaja TIDAK diabaikan — log eval fine-tune (finetune/*.log)
# memang bagian dari catatan kerja dan ikut di-commit.

